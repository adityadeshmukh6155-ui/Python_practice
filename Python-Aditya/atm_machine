pin = 1234
balance = 1500

print("Insert your card")

atm_pin = int(input("Enter your pin: "))

if pin == atm_pin:

    print("You entered ATM machine")

    while True:

        print("\n1. Check Balance")
        print("2. Withdraw Balance")
        print("3. Add Balance")
        print("4. Exit")

        option = int(input("Choose option: "))

        if option == 1:
            print("Your balance is:", balance)

        elif option == 2:

            withdraw = float(input("Enter withdraw amount: "))

            if withdraw <= balance:
                balance -= withdraw
                print("Current balance is:", balance)

            else:
                print("Insufficient balance")

        elif option == 3:

            adding = float(input("Enter adding amount: "))

            balance += adding

            print("New balance is:", balance)

        elif option == 4:
            print("Thank you for using ATM")
            break

        else:
            print("Invalid option")

else:
    print("Wrong PIN")





"generate_password"

password  = "Aditya"

while True:
    password = input("inter your password")

    if password == "Aditya":
        print("you are enter your system")
    
    elif password != "Aditya":
        print("please enter your correct password")

        
    
    else:
        print("your password is incorrect")

    break
